/**
 * Service client.
 *
 * Placeholder for now. This is where the calls to your services will live —
 * whichever transport you land on (gRPC-web, Connect, plain fetch, etc.).
 * Keep the transport details in here so your hooks stay clean and only ever
 * deal with the proto-derived types from `@/types/proto`.
 *
 * Example:
 *
 *   import type { Route } from '@/types/proto';
 *
 *   export const fetchRoutes = async (): Promise<Route[]> => {
 *     const res = await fetch('/api/routes');
 *     if (!res.ok) {
 *       throw new Error(`Request failed: ${res.status}`);
 *     }
 *     return (await res.json()) as Route[];
 *   };
 */

export {};
